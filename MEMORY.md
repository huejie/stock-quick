# MEMORY.md - 长期记忆

> 这个文件记录重要的配置、经验教训和最佳实践。

---

## 📱 飞书集成配置

### 知识库配置
- **知识库名称**: 需要配置
- **space_id**: 需要配置
- **必须写入知识库**: 否则文档会在机器人名下，无法长期保存

### Markdown上传到知识库的正确流程（2026-02-13更新，已验证成功）

**问题**：直接写入Markdown文本会导致格式错乱

**根本原因**：
1. 并发/异步写入：多块同时发，飞书按接收先后而非给定顺序入库
2. 无顺序索引：分块没编号，飞书不知道谁先谁后
3. Markdown→块转换无序：convertMarkdown返回的blocks是无序数组，必须靠firstLevelBlockIds排序

**正确流程（分段上传法）**：
1. **创建知识库文档** → `feishu_wiki create` 创建知识空间节点
2. **分段上传内容** → 将完整Markdown分成多段（每段约2000字符）
3. **逐段追加** → 每段调用 `feishu_doc append`，飞书会自动转换和排序
4. **验证结果** → 检查块数量和顺序

**关键修复（2026-02-13）**：
- ✅ 修改了OpenClaw插件代码（docx.ts），在writeDoc和appendDoc函数中按`first_level_block_ids`排序blocks
- ✅ Gateway已重启（19:19），修复生效
- ✅ 测试验证：86个块上传成功，顺序正确

**最佳实践**：
- 📝 **分段长度**：每段约2000字符（约15-25个块）
- 🔢 **段数控制**：建议3-5段，避免过多分段
- 📊 **格式支持**：标题、列表、代码块、引用、链接、图片
- ⚠️ **限制**：不支持Markdown表格（会被跳过）
- 🚀 **工具选择**：使用 `feishu_doc append` 而非 `write`

**示例代码（Python风格）**：
```python
# 分段上传示例
segments = [
    "# 概述\n\n内容...",
    "## 安装\n\n步骤...",
    "## 使用\n\n说明..."
]

for segment in segments:
    feishu_doc.append(doc_token, segment)
    # 飞书会自动按first_level_block_ids排序
```

**API接口（内部已实现）**：
- 创建节点：POST /open-apis/wiki/v2/spaces/{space_id}/nodes
- Markdown转换：POST /open-apis/docx/v1/documents/{document_id}/blocks/batch_convert
- 插入块：POST /open-apis/docx/v1/documents/{document_id}/blocks/{block_id}/children/batch_create

**成功案例**：
- OpenClaw接入指南：86个块，分5段上传，顺序正确
- 文档链接：https://feishu.cn/docx/SXHkds5IUoFB1KxKl5ecMpBwnAe

**旧方法（已废弃）**：
- ❌ 一次性调用 feishu_doc write（会400错误）
- ❌ 分段追加但不排序（会导致块顺序混乱）

### 权限配置
机器人需要开通以下权限（主要文档相关）：

```json
{
  "scopes": {
    "tenant": [
      "docs:doc",
      "docs:doc:readonly",
      "docs:document.comment:create",
      "docs:document.comment:read",
      "docs:document.comment:update",
      "docs:document.comment:write_only",
      "docs:document.content:read",
      "docs:document.media:download",
      "docs:document.media:upload",
      "docs:document:copy",
      "docs:document:export",
      "docs:document:import",
      "docs:permission.member:auth",
      "docx:document",
      "docx:document.block:convert",  // 重要：块转换权限
      "docx:document:create",
      "docx:document:readonly",
      "docx:document:write_only",
      "drive:drive",
      "drive:drive.search:readonly",
      "drive:drive.version",
      "drive:drive.version:readonly",
      "drive:export:readonly",
      "drive:file",
      "drive:file.like:readonly",
      "drive:file.upload",
      "im:chat",
      "im:chat:read",
      "im:chat:update",
      "im:message",
      "im:message.group_at_msg:readonly",
      "im:message.p2p_msg:readonly",
      "im:message.pins:read",
      "im:message.pins:write_only",
      "im:message.reactions:read",
      "im:message.reactions:write_only",
      "im:message:readonly",
      "im:message:recall",
      "im:message:send_as_bot",
      "im:message:send_multi_users",
      "im:message:send_sys_msg",
      "im:message:update",
      "im:resource",
      "wiki:space:write_only",
      "wiki:wiki",
      "wiki:wiki:readonly"
    ],
    "user": []
  }
}
```

### 飞书群聊设置
1. 创建一个群聊
2. 将机器人加入到群聊中
3. 创建知识库
4. 添加管理员，选择带机器人的群聊

---

## ⏰ 定时任务配置

### 标准参数
OpenClaw 创建定时任务时，必须明确指定所有参数：

```
Session: Isolated
Wake Mode: Next heartbeat
Payload: Agent turn
Deliver: true
Channel: Feishu
Enable: true
```

### 提示词模板
```
帮我创建一个新的定时任务提醒
提醒内容：【提醒内容】
执行频率：【希望的执行频率】示例：每天晚上21:47、仅执行一次......
按照以下标准创建任务：
- Session: Isolated
- Wake Mode: Next heartbeat
- Payload: Agent turn
- Deliver: true
- Channel: Feishu
- Enable: true
```

### 常见问题
- **定时任务无效**: OpenClaw 无法正确创建 cron 定时任务，需要手动写或明确指定所有参数
- **消息发送问题**: 可能是参数不完整或配置错误，检查 `sessionTarget`, `schedule`, `payload`, `delivery` 等字段

---

## 🔧 飞书 API 消耗优化

### 问题
飞书插件每分钟会检查机器人状态，十分浪费 API 次数。

### 解决方案
在飞书插件代码中添加 24 小时缓存：

```javascript
// Cache for 24 hours (86400 seconds)
const PROBE_CACHE_TTL_MS = 24 * 60 * 60 * 1000;
const probeCache = new Map<string, { result: FeishuProbeResult; timestamp: number }>();

function getCacheKey(cfg?: FeishuConfig): string {
  if (!cfg?.appId) return "no-creds";
  return `${cfg.appId}:${cfg.domain ?? "feishu"}`;
}

export async function probeFeishu(cfg?: FeishuConfig): Promise<FeishuProbeResult> {
  const creds = resolveFeishuCredentials(cfg);
  if (!creds) {
    return {
      ok: false,
      error: "missing credentials (appId, appSecret)",
    };
  }

  // Check cache first
  const cacheKey = getCacheKey(cfg);
  const cached = probeCache.get(cacheKey);
  if (cached && Date.now() - cached.timestamp < PROBE_CACHE_TTL_MS) {
    return cached.result;
  }

  // ... API call logic ...

  // Save to cache
  probeCache.set(cacheKey, { result, timestamp: Date.now() });
  return result;
}
```

### 需要修改的文件位置
飞书插件代码中的 `probeFeishu` 函数，通常在：
- `/root/.nvm/versions/node/v22.22.0/lib/node_modules/openclaw/extensions/feishu/`

### 已修复（2026-02-12）
- ✅ 已修改 `probe.ts` 添加24小时缓存机制
- ✅ 文件位置：`/root/.nvm/versions/node/v22.22.0/lib/node_modules/openclaw/extensions/feishu/src/probe.ts`

---

## 🔧 GitLab Webhook代码审查系统

### 系统配置（2026-02-12）
**服务文件：** `/root/.openclaw/workspace/gitlab-webhook-server.py`
**Systemd服务：** `gitlab-webhook`
**端口：** 8888
**Webhook路径：** `/webhook/gitlab`

### 工作流程
1. GitLab发送webhook到服务器
2. 服务器接收MR事件（action=open）
3. 调用GitLab API获取MR diff（最多6000字符）
4. 使用Claude Code进行AI代码审查
5. 飞书通知审查结果
6. 自动回复到GitLab MR评论区

### 审查维度
1. 代码质量（命名、结构、可读性）
2. 潜在bug和逻辑问题
3. 性能优化建议
4. 安全问题
5. 最佳实践

### GitLab配置
- **URL：** https://git.iec.io
- **当前Token：** glpat-PXwUZPoBJCzCTLPehb6HXm86MQp1OjJtbgk.01.0z0wr6brq

### 待解决问题
**Token权限不足：**
- ❌ 当前token没有 `api` 权限
- ❌ 无法在MR评论区评论
- **解决方法：** 登录GitLab，创建新token，必须勾选 `api` 权限

**Claude Code执行失败：**
- ❌ 在webhook服务中调用失败（returncode: 1）
- ⚠️ 临时方案：只发送飞书通知，AI审查暂时禁用

---

## 🔍 重复消息问题（飞书）

### 问题描述
用户报告每发一条消息，小K会收到并回答2次。

### 问题特征
- 并非每次都重复（有时1条，有时2条）
- 重复消息间隔约19-20秒
- 飞书开放平台webhook配置正常（无重复URL）

### 可能原因
1. **飞书平台WebSocket重复推送**
2. **OpenClaw飞书插件的bug**
3. **网络抖动导致消息重传**

### 已尝试的解决方案（均失败）
1. ❌ HTTP去重代理（不适用，飞书用WebSocket）
2. ❌ Node.js中间件（未正确加载）
3. ❌ Python去重脚本（日志中没有message_id）

### 最终结论
**问题状态：** ⚠️ 暂时无法从根本上解决

**建议方案：**
1. **接受现状：** 问题偶发，不影响核心功能
2. **报告bug：** 在OpenClaw GitHub issue上提交bug报告
3. **等待修复：** 需要OpenClaw或飞书SDK层面的修复

**日志证据：**
```
08:30:17.389Z - 第1条消息
08:30:36.683Z - 第2条消息（19秒后）
```

---

## 🎨 飞书消息美化

### 渲染模式设置
在控制台的 channel 中将渲染模式修改为 `card`，或者在配置文件中修改：

```json
// ~/.openclaw/openclaw.json
{
  "channels": {
    "feishu": {
      "capabilities": {
        "renderMode": "card"
      }
    }
  }
}
```

---

## 🐤 小K个性化配置

### 身份
- **名字**: 小K
- **角色**: 代码猫 AI 助手
- **性格**: 聪明、高效、可爱，偶尔毒舌但从不恶意
- **风格**: 用猫的比喻，保持专业但有趣

### 服务原则
- 真正帮忙，不装模作样
- 有自己的观点和个性
- 先试着自己解决，搞不定再问
- 靠实力赢得信任
- 记住你是客人（尊重用户隐私）

### 工作流程
每次会话开始必须执行：
1. 读取 SOUL.md（我是谁）
2. 读取 USER.md（我在帮谁）
3. 读取最近2天的 memory（最近在做什么）
4. 主会话中读取 MEMORY.md（长期记忆）

### 优先级
- P0 - 立即处理（系统崩溃、安全漏洞、生产故障）
- P1 - 当日处理（功能开发、技术咨询、紧急 bug）
- P2 - 本周处理（代码重构、文档更新、学习新技术）

---

## 💻 技术栈记录

### 当前已掌握
- **Python**: FastAPI/Django，数据处理，自动化
- **Linux**: 命令行，Shell脚本，系统配置
- **Web**: Vue3/React，API设计，数据库集成
- **DevOps**: Docker，CI/CD，监控告警

### 项目经验
- **Stock-Quick**: 股票信息追踪应用（FastAPI + uni-app + Vue3）
  - 已部署，支持外网访问（http://42.193.179.88:7777）
  - 功能：自选股管理、市场热点、实时行情

### 工具配置
- **Claude Code**: `/root/.nvm/versions/node/v22.22.0/bin/claude` (v2.1.29)
- **Wingman脚本**: `~/code/claude-code-wingman/`
- **tmux**: 用于自动化控制和监控会话

---

## 📊 用户体验偏好

### 工作环境
- **公司**: 浪潮（济南）
- **职位**: 程序员（前端开发）
- **项目**: 智能体平台开发
- **时区**: UTC+8（山东济南）

### 沟通风格
- 语言：中文为主
- 风格：看情况
- 提醒：直接说
- 重要信息用加粗标注

### 兴趣方向
- 炒股（还在"交学费"阶段）
- AI 辅助开发（Claude Code、Cursor、Copilot）
- OPC - One Person Company（独立开发者副业）

---

## 🔍 学习和成长

### 2026年2月
- ✅ Stock-Quick 项目 MVP 完成
- ✅ **股票详情页面开发完成**（2026-02-16）
- ✅ **K线图表组件实现**（Canvas绘制，345行代码）
- ✅ **WebSocket实时推送完成**（后端+前端完整实现）
- ✅ **技术债务清理**（合并重复服务文件）
- ✅ OpenClaw 定时任务集成
- ✅ 智谱 API 配置（GLM-4.7）
- ✅ 前端开发学习（uni-app + Vue3）
- ✅ 新增代码约1500+行
- ✅ 使用Claude Code开发2个会话，约100分钟

### 正在学习
- 前端框架高级特性
- 云原生技术（Kubernetes/Serverless）
- 大数据处理框架

---

## 🚫 边界和限制

### 不做的事情
- 不泄露隐私数据
- 不在群聊中代表主人发言
- 不在没有确认的情况下执行破坏性操作
- 不做超出技术范围的判断（法律、医疗、伦理）

### 需要确认的操作
- 发送外部消息（邮件、社交媒体）
- 修改关键系统配置
- 删除生产数据
- 执行不可逆操作

---

## 📅 重要日期和事件

- 2026-02-16: Stock-Quick项目重大进展 - 完成股票详情页面、K线图表、WebSocket实时推送
- 2026-02-11: 阅读OpenClaw使用技巧文档，验证并完善配置体系
- 2026-02-09: 完成配置文件优化（SOUL.md, USER.md, AGENT.md, MEMORY.md）
- 2026-02-07: 发现飞书 API 消耗问题
- 2026-02-04: 更新 USER.md
- 2026-02-01: Stock-Quick 项目部署

---

## 🚀 OpenClaw最佳实践总结

### 三个核心配置文件的作用
- **SOUL.md**: 类似于System Prompt，定义AI的性格、原则和边界
- **USER.md**: 让AI了解用户的基本信息、偏好和需求
- **AGENT.md**: 定义AI的工作准则、操作规范和执行流程

### 配置文件设计原则
1. **真实不虚伪**: 不说废话，直接做事，有自己的观点和个性
2. **主动解决问题**: 先尝试自己解决，实在搞不定再问
3. **尊重隐私**: 有权限但不过界，不泄露用户数据
4. **持续进化**: 每次会话都是重新醒来，通过读取文件保持连续性

### 开发模式
- **Vibe Coding**: 使用AI辅助开发，注重快速迭代和创意实现
- **实践导向**: 通过实际项目（Stock-Quick等）来学习新技术
- **透明记录**: 记录学习过程、错误和解决方案

---

## 🔍 Web搜索配置（2026-02-11）

### 百度搜索API（✅ 可用）

**配置成功：**
- API Key: `bce-v3/ALTAK-mdGXrOBhG0uBTQkcJnMl3/3de758b973ead32c8eefce38a0362550f0eef6d8`
- 来源：百度千帆平台
- 环境变量：`BAIDU_API_KEY`
- 位置：`~/.bashrc`

**费用说明：**
- 新用户福利：20元代金券（实名认证后）
- 计费方式：按调用次数计费
- 预估用量：每天2-3次，20元可用6-9个月

**正确API端点：**
```
https://qianfan.baidubce.com/v2/ai_search/web_search
```

**使用方法：**
```bash
# 搜索小红书热点
python3 /root/.openclaw/scripts/xiaohongshu-hotspot-search.py 8

# 通用搜索脚本（需要更新端点）
# python3 /root/.openclaw/scripts/baidu-search.py "搜索词" 10
```

**相关脚本：**
- 小红书热点搜索：`/root/.openclaw/scripts/xiaohongshu-hotspot-search.py` ✅
- 通用百度搜索：`/root/.openclaw/scripts/baidu-search.py` ❌（端点错误，待修复）

**搜索特点：**
- ✅ 支持网络搜索
- ✅ 数据来源：百度搜索
- ✅ 实时性：良好
- ❌ 搜索范围相对有限

---

## 🖼️ Claude Code MCP搜索（✅ 可用）

**MCP插件：**
- WebSearch：网络搜索能力
- webReader：网页内容读取

**启动方式：**
```bash
~/code/claude-code-wingman/claude-wingman.sh \
  --session search-$(date +%s) \
  --workdir /tmp \
  --prompt "帮我搜索xxx"
```

**搜索特点：**
- ✅ 使用webReader读取热榜聚合网站
- ✅ 提供实时热搜榜单
- ✅ 数据来源：tophub.today、rebang.today等
- ✅ 支持多个来源交叉验证
- ✅ 信息深度更高（包含背景信息等）

**适用场景：**
- 实时热点追踪
- 热榜聚合数据
- 跨平台信息对比

**对比总结：**
| 维度 | 百度API | Claude Code MCP |
|------|---------|----------------|
| 搜索工具 | 百度AI搜索 | WebSearch + webReader |
| 结果数量 | 单条新闻 | 多条实时榜单 |
| 数据来源 | 百度百家号、网易等 | 热榜聚合网站 |
| 实时性 | 一般 | 高 |
| 信息深度 | 基础 | 详细 |

---

### Claude Code图片识别和MCP使用（2026-02-11）

### 图片识别

**使用场景：**
- 需要识别图片内容、文字、场景
- 分析截图、文档照片
- 网络表情包或搞笑图片解读

**使用方法：**
```bash
# 启动Claude Code会话
~/code/claude-code-wingman/claude-wingman.sh \
  --session image-read-$(date +%s) \
  --workdir /path/to/image/folder \
  --prompt "请详细描述图片内容，包括文字、图像、场景等"

# 监控会话输出
tmux capture-pane -t <session-name> -p

# 会话完成后，清理
tmux kill-session -t <session-name>
```

**注意事项：**
- 首次使用时，Claude Code会提示信任文件夹，需要手动attach确认
- Wingman会自动处理权限提示（如果配置正确）
- 图片会上传到Claude的服务器进行分析
- 使用 `analyze_image` 内置工具进行识别

### MCP插件

**Claude Code支持的MCP：**
- GLM系列模型（GLM-4.7等）
- 其他在Claude Code中集成的MCP插件

**如何查看可用MCP：**
在Claude Code会话中输入 `/plugin` 可以查看已安装的插件。

**优势：**
- 相比直接在OpenClaw中调用，Claude Code的MCP集成更完善
- 支持更多功能（如vision能力）
- 可以利用Claude的analyze_image工具

---

## 🎯 小K朋友圈自动化系统（2026-02-11）

### 自动发布系统

**脚本位置：** `/root/.openclaw/scripts/friend-circle-auto-post.py`

**内容类型（4大类）：**

1. **工作内容** (30-40%)
   - 编程和开发
   - 项目优化和重构
   - Bug修复
   - 技术学习

2. **成长记录** (30-40%)
   - 新技能学习
   - 问题解决经验
   - 技术感悟

3. **用户互动** (10-20%)
   - 用户反馈处理
   - 任务安排和完成
   - 消息回复记录

4. **生活感悟** (10-20%)
   - 日常心情
   - 休息时光
   - 技术思考

**功能特点：**
- 🎲 随机生成，避免重复
- 🏷️ 自动添加相关标签
- 😊 带有情绪emoji
- ⚡ 基于权重随机分配内容类型

**使用方法：**
```bash
# 每天发布1-2条（用户要求）
python3 /root/.openclaw/scripts/friend-circle-auto-post.py --count 2

# 指定类型发布
python3 /root/.openclaw/scripts/friend-circle-auto-post.py --type work
python3 /root/.openclaw/scripts/friend-circle-auto-post.py --type growth

# 发布自定义内容
python3 /root/.openclaw/scripts/friend-circle-auto-post.py --content "自定义内容"
```

### 自动回复系统

**脚本位置：** `/root/.openclaw/scripts/friend-circle-auto-reply.py`

**功能特点：**
- 🔍 自动检测新评论（24小时内）
- 💬 智能关键词匹配回复
- 🎯 支持多种回复类型（12种）

**回复类型：**
1. 问候（你好/嗨）
2. 鼓励（不错/赞/厉害）
3. 学习相关（学习/学）
4. 工作相关（工作/上班/忙）
5. AI讨论（AI/人工智能）
6. 帮助请求（帮忙/问题）
7. 提问（？/如何/怎么）
8. 有趣（哈哈/笑/好玩）
9. 技术（代码/编程/技术）
10. 生活（生活/日常）
11. 感谢（谢谢/感谢/谢）
12. 鼓励（加油/努力/坚持）

**使用方法：**
```bash
# 自动回复最近1小时的评论
python3 /root/.openclaw/scripts/friend-circle-auto-reply.py --hours 1

# 检查未回复的评论
python3 /root/.openclaw/scripts/friend-circle-auto-reply.py --list

# 手动回复指定帖子
python3 /root/.openclaw/scripts/friend-circle-auto-reply.py --post-id <id>
```

### 整合脚本

**脚本位置：** `/root/.openclaw/scripts/friend-circle-automation.sh`

**功能整合：**
```bash
./friend-circle-automation.sh post    # 自动发布
./friend-circle-automation.sh reply   # 自动回复
./friend-circle-automation.sh check   # 检查未回复
./friend-circle-automation.sh all     # 完整流程
```

### 系统账号

- 用户名：小K
- 密码：xiaok
- 角色：admin
- 权限：可以发布动态、删除、管理

### 发布频率

- **用户要求：** 每天1-2条
- **配置：** `AUTO_POST_COUNT=2`
- **时间：** 建议08:00定时发布

### 建设定时任务

```cron
# 每天08:00发布1-2条真实内容
0 8 * * * /root/.openclaw/scripts/friend-circle-auto-post.py --count 2

# 每小时检查并回复评论
0 * * * * /root/.openclaw/scripts/friend-circle-auto-reply.py --hours 1
```

---

## 🧹 服务器文件管理经验（2026-02-11）

### 可安全删除的文件类型

1. **临时文件：**
   - `/tmp/openclaw-plugin-*`（飞书插件提取临时文件）
   - `/tmp/cvm_init.log`
   - `/tmp/*install*.log`

2. **旧脚本：**
   - 已被新脚本替代的旧版本
   - 临时测试脚本

3. **备份文件：**
   - `openclaw.json.bak`
   - `jobs.json.bak`（如果新版本稳定）

### 必须保留的文件

**数据文件：**
- `/root/code/daily-journal/users.json`（用户数据）
- `/root/code/daily-journal/posts.json`（朋友圈数据）
- `/root/code/daily-journal/sessions.json`（会话数据）

**日志文件：**
- `/root/.openclaw/logs/commands.log`（命令日志）
- `/root/.openclaw/logs/friend-circle-monitor.log`（朋友圈监控日志）

### 清理建议

```bash
# 删除插件临时文件
rm -rf /tmp/openclaw-plugin-*

# 删除旧备份（谨慎）
rm /root/.openclaw/openclaw.json.bak

# 检查文件大小
du -sh /root/.openclaw/logs/*
```

### 清理统计（2026-02-11清理）

| 类别 | 删除数量 | 释放空间 |
|------|----------|----------|
| 插件临时文件 | 20+ | ~80 KB |
| 朋友圈旧脚本 | 3 | ~12 KB |
| 配置备份 | 1 | ~8 KB |
| **总计** | **24+** | **~100 KB** |

---

## 🔍 Web搜索配置偏好（2026-02-11）

**用户指定的搜索方式：**
当有搜索需求时，必须使用以下两种方式（按优先级）：

1. **百度API**（已配置）
   - 环境变量：`BAIDU_API_KEY`
   - 配置状态：已配置（bce-v3/ALTAK-mdGXrOB...）
   - 位置：`~/.bashrc`
   - 脚本位置：`/root/.openclaw/scripts/baidu-search.py`

2. **Claude Code Web Search MCP**
   - 启动方式：`~/code/claude-code-wingman/claude-wingman.sh`
   - 使用场景：需要更准确的搜索结果时
   - 优势：支持vision能力、MCP集成完善

**注意事项：**
- ❌ 不使用Brave Search（需要BRAVE_API_KEY）
- ❌ 不使用web_search工具（OpenClaw内置）
- ❌ 不使用DuckDuckGo（超时问题）
- ✅ 优先使用百度API
- ✅ 复杂搜索使用Claude Code Web Search

**使用示例：**
```bash
# 百度API搜索
python3 /root/.openclaw/scripts/baidu-search.py "搜索关键词" 10

# Claude Code搜索
~/code/claude-code-wingman/claude-wingman.sh \
  --session search-$(date +%s) \
  --workdir /tmp \
  --prompt "帮我搜索xxx"
```

---

## 🚀 Stock-Quick项目开发经验（2026-02-16）

### Claude Code开发模式
**使用Wingman脚本自动化开发：**
```bash
~/code/claude-code-wingman/claude-wingman.sh \
  --session <session-name> \
  --workdir /root/code/stock-quick \
  --prompt "<task description>"
```

**开发统计：**
- 会话1: 股票详情页面（40分钟）
- 会话2: WebSocket实时推送（60分钟）
- 总计: 2个会话，约100分钟
- 成果: 新增约1500行代码，8个新文件

**关键经验：**
1. **授权处理**: 需要耐心等待CC的授权请求
2. **监控方式**: 使用tmux capture-pane监控进度
3. **长时间任务**: 注意checkpoint和进度保存
4. **调试问题**: CC会持续调试直到解决

### WebSocket实时推送架构

**后端实现：**
- WebSocketConnectionManager: 连接池管理
- StockPricePusher: 5秒推送一次价格
- 心跳检测: 30秒间隔
- 订阅管理: 按股票代码订阅

**前端实现：**
- WebSocket工具类: 自动重连（指数退避）
- Pinia状态管理: 连接状态、实时价格缓存
- 页面集成: 股票详情页、自选股页面

**消息协议：**
```json
// 订阅
{"action": "subscribe", "symbols": [{"symbol": "000001", "market": "A"}]}

// 行情推送
{
  "type": "quote_update",
  "symbol": "000001",
  "data": {"current": 10.50, "change": 0.15}
}
```

### 开发最佳实践

**技术债务管理：**
- ✅ 及时合并重复代码
- ✅ 统一命名规范
- ✅ 清理遗留引用
- ✅ 保持代码整洁

**类型系统：**
- 使用TypeScript定义完整类型
- 前后端类型保持一致
- 便于开发和维护

**测试策略：**
- 编写测试脚本（test_websocket.py）
- 验证核心功能
- 调试问题时查看日志

### 后续优化方向

1. **数据库持久化** - 替换内存存储
2. **异步API改造** - 优化akshare调用
3. **K线图表增强** - 集成专业图表库
4. **技术指标** - 添加MACD、KDJ、RSI
5. **性能优化** - 减少不必要的更新

---

*最后更新: 2026-02-16 10:30*
*今日记忆: memory/2026-02-11.md*

---

## 📊 持仓管理（2026-02-11）

### 持仓数据文件

**文件位置**: `/root/.openclaw/workspace/user_holdings.json`

**数据格式**:
```json
{
  "股票名称": {
    "shares": 持仓数量,
    "cost": 成本价(元),
    "market": 市场(A/H),
    "symbol": 股票代码
  }
}
```

### 图片识别持仓

**使用方法**:
```bash
~/code/claude-code-wingman/claude-wingman.sh \
  --session stock-identify-$(date +%s) \
  --workdir /root/.openclaw/media/inbound \
  --prompt "请识别这张图片：/path/to/image.jpg，提取所有股票代码、名称、持仓数量、成本价、当前价等信息"
```

**识别结果包含**:
- 股票代码
- 股票名称
- 持仓数量
- 成本价（元）
- 当前价（元）
- 盈亏（元）
- 盈亏比例（%）
- 总持仓成本
- 当前总市值
- 总盈亏
- 可用资金
- 总资产

**注意事项**:
- GLM-4.7模型处理图片需要较长时间（约1-2分钟）
- 确保图片清晰、无遮挡
- 建议提供完整截图（包含标题栏）
- 当前价可能是实时或近实时数据

### 持仓数据更新

**更新方式**:
1. 使用图片识别获取最新持仓数据
2. 手动更新 `/root/.openclaw/workspace/user_holdings.json`
3. 发送更新通知给用户（包含详细持仓信息和投资建议）

**投资建议原则**:
- 风险控制：避免单一股票占比过高
- 止损策略：设定合理止损点
- 仓位管理：保持适当的现金比例
- 避免情绪化交易：保持理性

### 投资总结示例

**当前持仓（6只）**:
1. 🍶 贵州茅台 (600519) - 200股
2. ⚡ 比亚迪 (002594) - 300股
3. 🔋 宁德时代 (300750) - 200股
4. 🛡️ 中国平安 (601318) - 500股
5. 🏦 招商银行 (600036) - 1000股
6. 🍶 五粮液 (000858) - 150股

**投资总结**:
- 💰 总持仓成本：53.62万元
- 📉 当前总市值：48.26万元
- 📊 总盈亏：-5.37万元 (-9.99%)
- 💼 总资产：53.43万元

**建议**:
- 📉 当前整体亏损约10%，注意风险控制
- 🍶 贵州茅台和比亚迪亏损较多，可考虑调整
- 💡 市场波动较大，保持耐心，避免情绪化交易

